<nav class="navbar navbar-expand-lg navbar-light bg-light">
<%= link_to 'Home',users_path %>
<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
</button>
<div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav mr-auto">
    <li class="nav-item active">
      <a class="nav-link" href="#">About As <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item active">
      <a class="nav-link" href="#">Contact As <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item active">
    <%= link_to 'Inbox Session', conversations_path,class:"nav-link" %>
    </li>
    
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <% @users.each do |user| %>
        <% if user.id == current_user.id %>
        <i class="icon-user"></i><%= user.lastName %></i>
        <% end %>
          <% end %>
          
      </a>
      <div class="dropdown-menu" aria-labelledby="navbarDropdown">
      <%= link_to "Profile",user_path(current_user.id)%>
        
        <div class="dropdown-divider"></div>
        <%= link_to "Logout", session_path(current_user.id), method: :delete %>
        
      </div>
     
    </li>
    <li class="nav-item">
      <%= link_to pluralize(Message.where('read=false').count,'Notification'), notifications_path,class:"nav-link disabled" %>
    </li>
  </ul>
  <form class="form-inline my-2 my-lg-0">
  <%= search_form_for @search do |form| %>
    <%= form.text_field :firstName_or_expertise_cont, class:"form-control mr-sm-2",placeholder:"Search"%>
    <div class="actions1"><%= form.submit "search",class:"btn btn-outline-success my-2 my-sm-0", type:"submit" %></div>
  <%end%>
  </form>
</div>
</nav>
<p id="notice"><%= notice %></p>
<div class="card" style="width: 25rem;margin-left: 30rem;" >
    <% @users.each do |user| %>
      <% if user.id != current_user.id %>
      <div class="card-header"><%= link_to user.firstName%></div>
      <div class="card-body" style="width: 25rem;">
      <h6> First Name</h6><%= user.lastName %></div>
      <div class="card-body" style="width: 25rem;"><h6> Last Name</h6><%= user.firstName %></div>
      <div class="card-body" style="width: 25rem;"><h6> Email</h6> <%= user.email %></div>
      <div class="card-body" style="width: 25rem;"><h6> Address</h6> <%= user.address %></div>
      <div class="card-body" style="width: 25rem;"><h6> Occupation</h6> <%= user.occupation %></div>
      <div class="card-body" style="width: 25rem;"><h6> Expertise</h6> <%= user.expertise %></div>
      <div class="card-body" style="width: 25rem;"><h6> Bio</h6> <%= user.bio %></dit>
      </div>
      <div class="card-footer" >
       <%= link_to user.firstName, user,class:"btn btn-outline-info" %>
        <%= link_to 'All Session', conversations_path(sender_id: current_user.id, recipient_id: user.id), method: :post,class:"btn btn-outline-info" %>
        </div>
       <br/>
       <% end %>
    <% end %>
 
    </div


        


